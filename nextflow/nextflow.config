// nextflow.config
manifest {
    description = 'Co-expression Network Pipeline with corinet'
    author = 'Liane Muir'
    mainScript = 'main.nf'
}

params {
    counts = 'data/test/counts.tsv'
}

// Work directory
workDir = './work'

// Profiles 
profiles {
    docker {
        docker.enabled = true
        process.container = 'net-lmuir:0.0.1'
    }
    conda {
        conda.enabled = true
        process.conda = 'environment.yml'
    }
}
