% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{net_export}
\alias{net_export}
\title{Export network results as TSV files}
\usage{
net_export(
  cor_mat,
  network,
  net_summary,
  modules,
  module_df,
  cor_threshold = 0.7
)
}
\arguments{
\item{cor_mat}{A gene-gene correlation matrix}

\item{network}{A network object output by build_network()}

\item{net_summary}{A data frame of global network statistics}

\item{modules}{A named integer vector mapping genes to module IDs}

\item{module_df}{A data frame of gene-module assignments
with node-level metadata (e.g. degree)}

\item{cor_threshold}{Correlation threshold used to define significant edges
(default: 0.7; should match the value passed to build_adjacency_mat())}
}
\value{
A character vector of exported file names
}
\description{
Writes correlation edges, network summary statistics, hub genes,
module assignments, node statistics, and module summaries to TSV files
}
\examples{
data(example_se)
mat <- prepare_expression_matrix(example_se)
cor_mat <- pairwise_corr(mat)
adj_mat <- build_adjacency_mat(cor_mat)
net <- build_network(adj_mat)
gene_mods <- detect_network_modules(net)
net_summary <- summarize_network(net, gene_mods$modules, gene_mods$modularity)
network_results <- net_export(cor_mat, net, net_summary, gene_mods$modules,
    gene_mods$module_df, cor_threshold = 0.7)
}
